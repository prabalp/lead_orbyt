name: orbyt
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - scrapling[all]==0.4.14
      - mcp==2.0.0
      - uvicorn
      - httpx
      - python-dotenv
      - pyjwt[crypto]
      - scikit-learn
      - python-multipart
      - pytest
      - pytest-asyncio
      - pytest-mock
